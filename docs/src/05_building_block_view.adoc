ifndef::imagesdir[:imagesdir: ../images]

[[section-building-block-view]]
== Building Block View

=== Whitebox Overall System

==== Overview Diagram

[plantuml, level1-overview, png]
----
@startuml
!include <C4/C4_Component>

skinparam componentStyle rectangle

actor "User" as user

package "Yovi System" {
  component "Nginx\n(API Gateway)" as nginx <<reverse proxy>>
  component "React Frontend\n(Webapp)\n(Vite + TS)" as frontend <<application>>
  component "Auth Service\n(Node.js + Express + TS)" as auth <<service>>
  component "Users Service\n(Node.js + Express + TS)" as users <<service>>
  component "Game Service\n(Node.js + Express + TS)" as gameservice <<service>>
  component "Gamey\n(Rust + Axum)" as gamey <<service>>
  database "authdb\n(PostgreSQL)" as authdb <<database>>
  database "users.db\n(SQLite)" as usersdb <<database>>
  database "gamedb\n(PostgreSQL)" as gamedb <<database>>
  database "Redis\n(In-memory store)" as redis <<database>>
}

user -down-> nginx : HTTPS (443)\nREST/JSON\nSocket.IO\nHTTP 80 redirect

nginx -down-> frontend : Serve static files
nginx -down-> auth : HTTP (3001)\n/api/auth/*
nginx -down-> users : HTTP (3000)\n/api/users/*
nginx -down-> gameservice : HTTP (3002)\n/api/game/*\n/api/game/socket.io/*
nginx -down-> gamey : HTTP (4000)\n/api/gamey/*

frontend ..> nginx : API calls from browser
users ..> auth : POST /api/auth/verify
gameservice ..> auth : POST /api/auth/verify
auth -down-> authdb : PostgreSQL\nSQL
users -down-> usersdb : SQLite\nSQL
gameservice -down-> gamedb : PostgreSQL\nSQL
gameservice -down-> redis : Redis protocol\nMatchmaking + sessions

note right of nginx
  TLS termination
  Rate limiting + CORS
  WebSocket upgrade
  blocks external /api/auth/verify
end note

note right of users
  Profile CRUD
  Delegated auth verification
  Metrics endpoint
end note

note right of gameservice
  Match persistence
  Rankings + stats
  Matchmaking + realtime
  Metrics endpoint
end note

note right of gamey
  Stateless rules engine
  Classic and neural bots
  Metrics endpoint
end note

@enduml
----

**Motivation**

The system is decomposed into focused subsystems so that persistence, realtime orchestration, authentication and game computation can evolve independently:

* **Nginx** is the only public ingress and owns cross-cutting HTTP concerns.
* **React Frontend** owns the browser UX and client-side orchestration.
* **Auth Service** owns identity, credentials, refresh tokens and token verification.
* **Users Service** owns profile data and delegates JWT validation to Auth Service.
* **Game Service** owns durable game state, rankings, matchmaking and Socket.IO sessions.
* **Gamey** owns Y rules and bot move computation and remains stateless.
* **Redis** stores only transient online state.

**Contained Building Blocks**

[cols="1,3", options="header"]
|===
| Building Block | Responsibility

| **Nginx (API Gateway)**
a|
* Single public ingress via `443` with `80` redirect
* Routes `/api/auth/*`, `/api/users/*`, `/api/game/*`, `/api/game/socket.io/*`, `/api/gamey/*`, `/prometheus/`, `/grafana/` and frontend assets
* Handles CORS, rate limiting and WebSocket upgrade
* Blocks public access to `/api/auth/verify`

| **React Frontend (Webapp)**
a|
* Runs entirely in the user's browser
* Renders board, matchmaking, rankings and profile views
* Stores session state and access token client-side
* Calls backend APIs only through Nginx
* Opens Socket.IO connections for online play

| **Auth Service**
a|
* Owns `authdb`
* Handles register, login, refresh, logout and logout-all
* Verifies access tokens for other services through `/api/auth/verify`
* Exposes Prometheus metrics at `/metrics`

| **Users Service**
a|
* Owns `users.db`
* Manages user profiles under `/api/users/profiles/*`
* Uses `verifyJwtMiddleware` plus `AuthVerifyClient` to validate bearer tokens through Auth Service
* Exposes Prometheus metrics at `/metrics`

| **Game Service**
a|
* Owns `gamedb` and Redis
* Protects all `/api/game/*` routes with centralized auth verification
* Persists matches, moves, rankings and statistics
* Owns matchmaking, online sessions, reconnect grace and turn timeouts
* Emits Socket.IO events and exposes metrics at `/metrics`

| **Gamey**
a|
* Implements core Y rules, board evaluation and bot selection
* Exposes `/status`, `/metrics`, `/v1/ybot/choose/:botId` and `/v1/ybot/play`
* Maps aliases such as `medium` to `minimax_balanced_d2`
* Has no database access and remains fully stateless

| **Redis**
a|
* Ephemeral store used only by Game Service
* Keeps queue entries, active sessions and Socket.IO pub/sub state
* Durable history always lives in `gamedb`

| **authdb**
a|
* Stores credentials, refresh tokens and token metadata
* Accessible only through Auth Service

| **users.db**
a|
* Stores user profiles and preferences
* Accessible only through Users Service

| **gamedb**
a|
* Stores matches, move history, rankings and statistics
* Accessible only through Game Service
|===

=== Level 2

==== White Box: Auth Service

[plantuml, level2-auth, png]
----
@startuml
package "Auth Service (Node.js + TypeScript)" {

  package "Controllers" <<frame>> {
    component [AuthController] <<TS>>
  }

  package "Services" <<frame>> {
    component [AuthService] <<TS>>
  }

  package "Repositories" <<frame>> {
    component [CredentialsRepository] <<TS>>
  }

  package "Middleware" <<frame>> {
    component [VerifyTokenMiddleware] <<TS>>
  }
}

database "authdb (PostgreSQL)" as authdb <<database>>

[AuthController] ..> [AuthService]
[AuthController] ..> [VerifyTokenMiddleware]
[AuthService] ..> [CredentialsRepository]
[CredentialsRepository] --> authdb

@enduml
----

[cols="1,3", options="header"]
|===
| Component | Responsibility

| **AuthController**
a|
* Exposes `register`, `login`, `refresh`, `logout`, `logout-all` and `verify`
* Starts per-route HTTP metrics timers
* Maps service errors to HTTP responses

| **AuthService**
a|
* Registers users
* Hashes and verifies passwords
* Issues and rotates tokens
* Maintains refresh-token lifecycle

| **CredentialsRepository**
a|
* Encapsulates `authdb` access
* Persists credentials and token metadata

| **VerifyTokenMiddleware**
a|
* Validates access tokens for `/api/auth/verify`
* Returns normalized claims for internal callers
|===

==== White Box: Users Service

[plantuml, level2-users, png]
----
@startuml
package "Users Service (Node.js + TypeScript)" {

  package "Controllers" <<frame>> {
    component [UsersController] <<TS>>
  }

  package "Services" <<frame>> {
    component [UserService] <<TS>>
    component [AuthVerifyClient] <<TS>>
  }

  package "Repositories" <<frame>> {
    component [UserRepository] <<TS>>
  }

  package "Middleware" <<frame>> {
    component [VerifyJwt] <<TS>>
  }
}

database "users.db (SQLite)" as usersdb <<database>>
component "Auth Service" as auth <<external>>

[UsersController] ..> [VerifyJwt]
[UsersController] ..> [UserService]
[VerifyJwt] ..> [AuthVerifyClient]
[UserService] ..> [UserRepository]
[UserRepository] --> usersdb
[AuthVerifyClient] --> auth

@enduml
----

[cols="1,3", options="header"]
|===
| Component | Responsibility

| **UsersController**
a|
* Exposes `POST /api/users/profiles`
* Exposes `GET /api/users/profiles/by-username/:username`
* Exposes `GET /api/users/profiles/:id`
* Exposes `PUT /api/users/profiles/:id`

| **UserService**
a|
* Implements profile CRUD logic
* Validates profile updates

| **UserRepository**
a|
* Reads and writes `users.db`
* Keeps SQL isolated from HTTP concerns

| **VerifyJwt**
a|
* Protects `/api/users/profiles/*`
* Reads bearer token from `Authorization`
* Attaches `userId` and `username` to the request

| **AuthVerifyClient**
a|
* Calls `POST /api/auth/verify`
* Uses a small in-memory cache and a short timeout
* Converts upstream failures into service-unavailable responses
|===

==== White Box: Game Service

[plantuml, level2-gameservice, png]
----
@startuml
package "Game Service (Node.js + TypeScript)" {

  package "Controllers" <<frame>> {
    component [GameController] <<TS>>
  }

  package "Services" <<frame>> {
    component [MatchService] <<TS>>
    component [StatsService] <<TS>>
    component [RankingService] <<TS>>
    component [MatchmakingService] <<TS>>
    component [OnlineSessionService] <<TS>>
    component [TurnTimerService] <<TS>>
    component [BotFallbackService] <<TS>>
    component [AuthVerifyClient] <<TS>>
  }

  package "Repositories" <<frame>> {
    component [MatchRepository] <<TS>>
    component [StatsRepository] <<TS>>
    component [RankingRepository] <<TS>>
    component [MatchmakingRepository] <<TS>>
    component [OnlineSessionRepository] <<TS>>
  }

  package "Realtime" <<frame>> {
    component [SocketServer] <<TS>>
  }

  package "Middleware" <<frame>> {
    component [VerifyJwt] <<TS>>
    component [ErrorHandler] <<TS>>
  }
}

database "gamedb (PostgreSQL)" as gamedb <<database>>
database "Redis" as redis <<database>>
component "Auth Service" as auth <<external>>
component "Gamey" as gamey <<external>>

[GameController] ..> [MatchService]
[GameController] ..> [StatsService]
[GameController] ..> [RankingService]
[GameController] ..> [MatchmakingService]
[GameController] ..> [OnlineSessionService]

[VerifyJwt] ..> [AuthVerifyClient]
[AuthVerifyClient] --> auth

[OnlineSessionService] ..> [TurnTimerService]
[OnlineSessionService] ..> [BotFallbackService]
[BotFallbackService] --> gamey

[MatchService] ..> [MatchRepository]
[MatchService] ..> [RankingService]
[StatsService] ..> [StatsRepository]
[RankingService] ..> [RankingRepository]
[MatchmakingService] ..> [MatchmakingRepository]
[OnlineSessionService] ..> [OnlineSessionRepository]

[MatchRepository] --> gamedb
[StatsRepository] --> gamedb
[RankingRepository] --> gamedb
[MatchmakingRepository] --> redis
[OnlineSessionRepository] --> redis

[SocketServer] ..> [MatchmakingService]
[SocketServer] ..> [OnlineSessionService]

@enduml
----

[cols="1,3", options="header"]
|===
| Component | Responsibility

| **GameController**
a|
* Exposes match, ranking, stats, matchmaking and online session REST endpoints
* Validates payloads and maps domain errors to HTTP

| **MatchService**
a|
* Creates matches and persists moves
* Finishes matches and triggers ranking updates
* Queues bot move work for AI matches

| **StatsService**
a|
* Aggregates player statistics and history views

| **RankingService**
a|
* Applies ELO updates
* Exposes leaderboard and per-user ranking

| **MatchmakingService**
a|
* Manages the Redis queue
* Pairs compatible players
* Emits `matchmaking:matched`

| **OnlineSessionService**
a|
* Creates and updates live session state
* Handles reconnect, abandon, chat and move application
* Emits `session:state`

| **TurnTimerService**
a|
* Tracks per-turn deadlines
* Notifies session logic when a turn expires

| **BotFallbackService**
a|
* Calls Gamey when a bot move is needed during online play
* Applies automatic moves after timeout or fallback conditions

| **VerifyJwt / AuthVerifyClient**
a|
* Validate tokens through Auth Service
* Protect the entire `/api/game` router

| **SocketServer**
a|
* Handles `queue:join`, `queue:cancel`, `match:join`, `move:play` and `chat:message`
* Uses the Redis adapter to support multi-instance delivery
|===

==== White Box: Gamey

[plantuml, level2-gamey, png]
----
@startuml
package "Gamey (Rust)" {
  component [Axum Router] <<RUST>>
  component [Bot Registry] <<RUST>>
  component [Minimax Bots] <<RUST>>
  component [Neural MCTS Bots] <<RUST>>
  component [Game Domain] <<RUST>>
  component [Metrics] <<RUST>>
}

[Axum Router] --> [Bot Registry]
[Axum Router] --> [Game Domain]
[Axum Router] --> [Metrics]
[Bot Registry] --> [Minimax Bots]
[Bot Registry] --> [Neural MCTS Bots]
[Minimax Bots] --> [Game Domain]
[Neural MCTS Bots] --> [Game Domain]

@enduml
----

[cols="1,3", options="header"]
|===
| Component | Responsibility

| **Axum Router**
a|
* Exposes `/status`, `/metrics`, `/v1/ybot/choose/:botId` and `/v1/ybot/play`
* Parses requests and serializes engine responses

| **Bot Registry**
a|
* Resolves aliases such as `easy`, `medium`, `hard`, `expert` and `expert_fast`
* Keeps the available bot implementations available to HTTP handlers

| **Minimax Bots**
a|
* Implement classic search-based play
* Include the balanced `medium` bot alias

| **Neural MCTS Bots**
a|
* Load the ONNX model at startup
* Power `expert` and `expert_fast`

| **Game Domain**
a|
* Owns board representation, rules, win detection and move application

| **Metrics**
a|
* Publishes bot move histograms and other Prometheus metrics
|===
